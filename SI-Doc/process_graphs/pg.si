# relationships between regions

(A <-> B) => position  # link A pos to B pos

# (A <-> B) => (position, scale)  # link A pos to B scale

C := region => type(composition) & regions := [     # linking graph compression, not yet implemented in silang
                        (A <-> B, position, scale),
                        (B -> C, position),
                        (D -> C, scale),
                        (D <-> E, position, scale)
                    ]
