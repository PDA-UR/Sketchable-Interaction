# needs EBNF
# needs lexical analysis (lexer)
# needs parsing and syntax checking (parser)
# needs code generation (code generator)
    # this however is not typical code generation as the result is loaded into a SI-context or the results builds a fully usable SI-context

